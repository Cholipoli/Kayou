import React from 'react';
import MapComponent from '../components/Map'; // Import the map component
const HomePage = () => {
  return (
    <div>
      <MapComponent /> {/* This will render the map */}
    </div>
  );
};

export default HomePage;
